using System;
using System.Collections.Generic;
using System.Text;

namespace Toolshed
{
    public enum TimeSpanPartType
    {
        All, //custom ones
        Official ///Only the parts offically supported by the BCL Milli, seconds, minutes, hours, days and years
    }
}
